# 仓库工作规范

## 作用域与目标

本文件适用于整个仓库，并补充全局 `~/.codex/AGENTS.md`。本仓库用于维护个人 Agent 工程环境的脱敏能力索引与配置快照，不是配置备份、安装器或密钥存储。

- `README.md` 是环境快照的唯一事实来源。
- 仅记录可公开的配置结构、能力名称、来源、状态和版本。
- 保持改动聚焦；未经用户明确要求，不安装、升级或删除 Skill、插件、MCP 服务、依赖或本地工具。

## 更新快照

1. 按 `README.md` 的“更新环境快照”一节执行只读盘点。
2. 先核实完整现状，再修改文档；不得根据缓存目录、当前会话能力或记忆推断启用状态。
3. 只有完成全部盘点后才更新快照日期；日期使用 `Asia/Shanghai` 时区。
4. 没有变化的条目保持原有措辞和顺序，避免无关重排或格式化。

## 统计口径

- **个人 Skills**：统计 `~/.agents/skills` 与 `~/.codex/skills` 下的个人 `SKILL.md`；排除 `~/.codex/skills/.system` 和插件包内的 Skills，重复安装不得重复计数。
- **插件包**：按 `~/.codex/plugins/cache` 中实际存在的 `.codex-plugin/plugin.json` 清单统计，并记录精确版本。
- **已启用插件**：仅统计 `~/.codex/config.toml` 中明确配置且 `enabled = true` 的插件。
- **MCP 服务**：统计顶层 `[mcp_servers.<name>]` 配置；不要把 `.env` 等子表重复计数，并单独说明禁用状态。
- **本地工具链**：以当前 shell 中 `command -v` 解析到的可信可执行文件及其 `--version` 输出为准。
- 始终区分“本机已缓存”“配置中已登记”“明确启用”和“当前会话已暴露”，不得混用这些状态。

## 敏感信息边界

- 不读取、打印、复制或提交令牌、密钥、凭据、环境变量值及带参数的服务地址。
- 检查 `config.toml` 时只读取完成统计所需的节名和非敏感状态字段；不要输出完整文件或 MCP 的命令、参数与环境变量内容。
- 若命令意外输出敏感值，立即停止传播并在记录或回复中脱敏。

## 验证

提交快照变更前至少确认：

- README 中的数量与只读盘点结果一致。
- 插件和 CLI 版本来自当前清单或可信可执行文件。
- 插件与 MCP 的状态符合配置，而不是仅依据缓存存在。
- `git diff --check` 通过，差异中不含敏感值或无关改动。
- 无法执行的检查或仍不确定的状态已明确报告，不以推测补全。
